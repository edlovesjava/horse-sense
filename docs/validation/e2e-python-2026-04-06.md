# E2E Validation: Python Project

> **Date**: 2026-04-06
> **Tester**: Ed Wentworth
> **Task**: T-026

## Setup

```bash
mkdir /tmp/test-python && cd /tmp/test-python
bash horse/scripts/new_project.sh my-api
python -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt
claude --plugin-dir /workspaces/horse-sense/horse
```

## Checklist

- [x] `new_project.sh` scaffolds directory structure
- [x] `.claude/config.json` created with `language: python`
- [x] `pyproject.toml`, `requirements.txt`, `requirements-dev.txt` generated
- [x] All `/horse:*` commands load (`/horse:guide`, `/horse:plan`, `/horse:audit`, etc.)
- [x] `/horse:guide` presents interactive kickoff flow
- [x] `ruff check .` passes
- [x] `mypy src/` passes
- [x] `pytest tests/` passes
- [x] Next-steps output shows concrete commands (no broken `$CLAUDE_PLUGIN_ROOT` refs)

## Issues Found During Validation

1. **conftest.py unused import** — `import pytest` caused ruff F401. Fixed in `4962d21`.
2. **Broken next-steps output** — `${CLAUDE_PLUGIN_ROOT}` not set outside Claude Code. Fixed in `6da2b47`.

## Result

**PASS** (after fixes)
